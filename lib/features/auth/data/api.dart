import 'package:flutter/material.dart';

class ApiConection extends StatelessWidget {
  const ApiConection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Column(
        children: [],
      ),
    );
  }
}
